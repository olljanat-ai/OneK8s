apiVersion: v1
kind: Service
metadata:
  name: {{ include "db-hello.name" . }}
  labels:
    {{- include "db-hello.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "db-hello.selectorLabels" . | nindent 4 }}
